using System;
using MinhaBiblioteca;

class Exercicio6
{
    static int [,] somarMatriz(int[,] matrizA, int[,] matrizB)
    {
        if(matrizA.GetLength(0) != matriz.GetLength(0) ||
        matrizA.GetLength(1) != matriz.GetLength(1))
        {
            Console.WriteLine("As matrizes não tem a mesma ordem!");
            return null;
        }
    }
}